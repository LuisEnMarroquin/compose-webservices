{
  "name": "nginx",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "start": "node server.js",
    "docker": "docker-compose -p nginx_docker up -d --build --force-recreate --remove-orphans",
    "docker:prune": "docker system prune --volumes",
    "upgrade:latest": "npm --depth 9999 update --dev"
  },
  "dependencies": {
    "compression": "^1.7.4",
    "cors": "^2.8.5",
    "express": "^4.17.1",
    "helmet": "^3.21.2",
    "mongoose": "^5.7.6"
  }
}
